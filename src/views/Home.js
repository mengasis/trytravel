import React from 'react'
import PropTypes from 'prop-types'
import logo from '../logo.svg'

const Home = () => {
	return (
		<div>
			<span>HOME</span>
			<img src={logo} />
		</div>
	)
}

Home.propTypes = {}

export default Home
